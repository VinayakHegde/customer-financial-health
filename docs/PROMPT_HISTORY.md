# Prompt History

This project was built using Cursor with AI-assisted development.

Raw Cursor transcripts are stored in:

- `.specstory/history/`

Curated session snapshots are stored in:

- `docs/ai/sessions/`

| Session | Purpose | Raw transcript | Snapshot | Commit(s) | Outcome |
|---|---|---|---|---|---|
| S000 | Next.js scaffold | `.specstory/history/2026-06-10_10-20-18Z-next.js-app-setup-with-typescript-and-tailwind-css.md` | `docs/ai/sessions/S000-nextjs-scaffold.md` | `88f3124`, `d97043e`, `b8a98c8` | App scaffolded; snapshot renumbered S001→S000 in `be002b4`; lint/typecheck/build pass |
| S001 | Task analysis | `.specstory/history/2026-06-10_10-49-02Z-task-analysis-for-ai-session-s001.md` | `docs/ai/sessions/S001-task-analysis.md` | `a23e372`, `be002b4`, `4173435`, `1140f85` | `docs/TASK_ANALYSIS.md` on `docs/task-analysis`; analysis only, no app code. **Artefact withdrawn in S003 D-14** — pre-brief analysis scoped a collections / arrangement-journey workflow the brief does not ask for; useful substance inlined into `docs/discovery/NOTES.md`. Snapshot preserved for prompt-history transparency. |
| S002 | AI guardrails (Rulesync) | `.specstory/history/2026-06-10_11-12-19Z-minimal-rulesync-setup-for-product-development.md` | `docs/ai/sessions/S002-ai-guardrails.md` | `922468e`, `38b2148`, `52699ba`, `ac6ff05`, `70489e0`, `3f460ae` on `ai-guardrails` | Minimal Rulesync scaffold (2 rules, 5 phase commands, 1 critic subagent, 1 phase-gate skill) targeting Cursor + Claude Code; generated tool files gitignored; CLAUDE.md untracked; no implementation rules yet |
| S003 | Discovery pass (+ corrective refinement + withdrawal of TASK_ANALYSIS.md) | `.specstory/history/2026-06-10_13-29-42Z-ophelos-engineering-take-home-task.md`, `.specstory/history/2026-06-10_15-57-27Z-discovery-notes-refinement-based-on-prd.md` | `docs/ai/sessions/S003-discovery.md` | (in progress) on `main` | `docs/discovery/NOTES.md` produced per phase-gate schema; brief is binding; PRD is source of truth for build scope; **OQ-1 RESOLVED (D-13 / D-14)** — pre-brief arrangement-journey items dropped (repayment-plan selection, arrangement confirmation, collections workflow, agent review processes, arrears-management tooling, repayment-arrangement booking, payment-plan confirmation, `POST /api/arrangements`, agent console / override UI, COps-specific payloads); **D-14:** `docs/TASK_ANALYSIS.md` withdrawn (file deleted), all useful substance inlined into `docs/discovery/NOTES.md` as repo-owned paraphrases; `.rulesync/commands/{discovery,prd}.md` and `docs/PRD_TEMPLATE.md` updated to remove the withdrawn input; submission artefacts (`README`, prompt history, `DECISIONS.md`, time-spent) promoted to first-class success signals + constraints; **OQ-2 through OQ-7 direction-set** — per-submission immutable snapshots; disposable + band + explainer + delta; canonical 4-case edge set; SQLite via lightweight ORM (snapshot history only; tech-spec picks ORM); 7-persona fixture set (surplus / breakeven / shortfall / zero-income / new-customer / irregular-income / joint-income); FCA paraphrases owned by NOTES.md and labelled "not independently verified" |
| S004 | PRD generation | `.specstory/history/2026-06-10_16-33-38Z-product-requirements-document-guidelines.md` | `docs/ai/sessions/S004-prd.md` | `0cca832` on `doc/prd` | `docs/PRD.md` produced per `docs/PRD_TEMPLATE.md` structure, using only the brief PDF and `docs/discovery/NOTES.md` as authoritative sources (withdrawn `docs/TASK_ANALYSIS.md` not cited); lean MVP scope with **R1–R17** (Must: R1–R4 Core + R14–R17 Submission; Should: R5–R10 Vulnerability/Regulatory; Could: R11–R13 Stretch); persistence committed at PRD level only (R3) with technology choice deferred to tech-spec; **N1–N8** non-goals enforce the §7(b) drops in PRD itself (no arrangements endpoint, no agent UI, no collections workflow, no auto-vulnerability classification); **Q1↔A1, Q2↔A2, Q3↔A3** — every open question paired with a working-assumption default (band thresholds default to DI > 0 / = 0 / < 0; first-snapshot delta uses a friendly placeholder; irregular income accepted per-month as-entered); FCA / GDPR paraphrases inherited from `NOTES.md` with "_not independently verified_" labels intact (OQ-7); Discovery → PRD traceability table closes every `NOTES.md` insight to one or more `R*` IDs |
| S006 | Rulesync command personas | `.specstory/history/2026-06-10_18-48-50Z-rulesync-command-file-updates.md` | `docs/ai/sessions/S006-rulesync-command-personas.md` | (in progress) on `main` | `## Operating persona` block added to all five `.rulesync/commands/*.md` (discovery / prd / tech-spec / test-plan / implement); tech-spec §3 reworded "Detailed sections" → "Implementation slices" and §6 rule extended to cover architectural layers + Trade-offs routing; implement Process expanded 5 → 8 steps to make TDD ordering explicit (failing tests first → minimum implementation → refactor only after green); **no review/critic command added** — `.rulesync/commands/` had none and adding one would have introduced a new agent surface against the user's explicit constraint; product docs (`docs/discovery/NOTES.md`, `docs/PRD.md`, `docs/PRD_TEMPLATE.md`), implementation code, `.rulesync/{rules,skills,subagents}/`, `.cursor/rules/`, and `AGENTS.md` all untouched |
| S005 | PRD critic review + minimum append-only follow-ups | `.specstory/history/2026-06-10_17-12-45Z-document-review-findings-and-recommendations.md` | `docs/ai/sessions/S005-prd-critic-and-followups.md` | (in progress) on `doc/prd` | Critic role (`.rulesync/subagents/critic.md`) reviewed `docs/PRD.md` across 7 focus areas — Verdict: **Minor fixes**, 24 findings recorded; user accepted 4 for append-only resolution (**F3.1** accessibility gap, **F1.1** stretch-test discipline, **F4.1** retention assumption, **F7.2** snapshot correction story); PRD modified append-only: **R18** (Should / Vulnerability — screen-reader + motor accessibility; conformance level deferred to tech-spec to avoid fabricating a WCAG citation), **R19** (Should / Core — stretch tests if any of R11/R12/R13 are delivered; doesn't edit R4), **Q4↔A4** (retention defaults to lifetime-of-customer-record for take-home; production retention out-of-scope per N8), **Q5↔A5** (corrections = new snapshot; prior snapshots remain visible; UI "looks like a correction" hint deferred to tech-spec); §9 traceability extended for R18 / R19 only (Q/A pairs follow the established Q1–Q3 convention of no §9 row); R1–R17, Q1–Q3, A1–A3, §1–§5, §7 unchanged; PRD passes `/tech-spec` pre-checks; ~20 critic findings consciously deferred — categorised in S005 snapshot as (a) tech-spec / test-plan responsibility, (b) would edit existing rows (user constraint), (c) would introduce new product scope (user constraint), (d) cosmetic; deferred set is the inbox for `/tech-spec` and `/test-plan` |
| S007 | Tech spec generation + two critic rounds + append-only PRD R20 | `.specstory/history/2026-06-10_<TBC>Z-tech-spec.md` (SpecStory writes on Cursor window close) | `docs/ai/sessions/S007-tech-spec.md` | (in progress) on `main` | Three iterations of `docs/TECH_SPEC.md` (rev 1 → rev 2 → rev 3) bracketed by two `@critic` rounds; final spec is 517 lines / 9 slices (S1 affordability domain, S2 SQLite+Drizzle persistence, S3 personas+cookie mock-auth, S4 `<DashboardView />`, S5 update form + Server Action + delta, S6 `<HistoryList />`, S7 test infrastructure + coverage matrix, S8 submission deliverables, S9 reflection-not-advice framing). **R20 added to PRD append-only** (Should / Regulatory, "tight" wording) when the round-1 critic flagged the inlined "not financial advice" footer as a workflow-rule-2 gate-cross; PRD now at R1–R20. **Round-1 critic** (Verdict: Minor fixes; F1.1–F8.3): top-3 = async-Server-Component-vs-Vitest, F4.3 framing gate-cross, free-text-`note` PII; resolved by routing F4.3 back to `/prd` (R20), deleting the `note` field as over-engineering, and refactoring S4/S5/S6 into async `page.tsx` + sync `<View />` component pairs so Vitest can render the sync surface. **Round-2 critic** (Verdict: Minor fixes; 28 findings F1.1–F9.3 — 4 High / 9 Medium / 11 Low / 4 suspicion-flagged): top-3 = slice-ordering bug (S9 was last but S4/S5/S6 imported from it), framing placement extended beyond R20's literal "every outcome screen", WCAG 2.2-specific SCs implicit (SC 3.3.7 / 2.5.8 / 2.4.11); user chose to address **all 28** in rev 3, with **drop** (rather than route-back-to-PRD) for the framing-placement gate-cross. Rev 3 changes: §3 intro rewritten — IDs append-only, recommended impl order `S7-setup → S1 → S2 → S3 → S9 → S4 → S5 → S6 → S8`; `<FramingNotice />` + `<SupportSignpost />` rendered **inside** the sync View components; S9 narrowed to `/dashboard` + `/history` only; WCAG 2.2 AA SCs pinned at §4 cross-cutting; S5 commits to SC 3.3.7 preserve-input-on-error-rerender; SC 3.3.4 satisfied by A5; S7 adds `withPersonaCookie()` helper + explicit `vi.mock('next/headers')` strategy; forbidden-token list labeled illustrative for `/test-plan`; §5 trade-offs extended (free-text-note removal extended to label residual risk; `useActionState` round-trip not unit-tested acknowledged); §7 traceability rewritten for R1/R5/R6/R7/R9. **Post-critic surgical fixes** (D-41 / D-42, "minimum change" guardrails): S2 schema `band TEXT NOT NULL` → `band TEXT` so the no-data outcome (S1 `Band \| null`) is persistable; S2 `listSnapshots` ordering comment reconciled with S6 (newest → oldest, matches the existing index). Spec passes `/test-plan` pre-checks; handoff inbox for `/test-plan` carries the S005 deferred items (F3.4 / F5.5 / F7.3 / F7.6) plus the forbidden-token-list finalisation. Decisions D-34 → D-42 in S007 snapshot. |
| S008 | Test plan generation + critic follow-up | `.specstory/history/2026-06-10_21-20-39Z-test-plan-for-technical-specification.md` | `docs/ai/sessions/S008-test-plan.md` | (uncommitted) on `main` | **Phase 4 complete.** `docs/TEST_PLAN.md` (632 lines): 45 test cases T1–T14, T18–T45 (T15–T17 reserved); every Must (R1–R4, R14–R17) and in-scope Should (R5–R10, R18, R20) covered; explicit gaps for R11/R12/R13 and R19; forbidden token lists finalised; S005 deferred items closed (F3.4/F5.5/F7.3/F7.6). `@critic` Verdict: **Minor fixes** — user applied 9-item test-plan-only patch: internal refs, T1 layer fix, T44/T45 added, persona-to-branch table, T8 `irregularIncomeNote`, T24 focus, T12/T20 label logging, §6 known limits. No PRD/tech-spec/code changes. Passes `/implement` gate. Handoff: **S009 `/implement S7-setup`**. Decisions D-43 → D-56. |
| S009 | `/implement S7-setup` — Vitest harness + shared test helpers | `.specstory/history/2026-06-10_22-06-35Z-s7-setup-implementation.md` | `docs/ai/sessions/S009-implement-s7-setup.md` | (uncommitted) on `main` | **Complete.** S7-setup shipped: Vitest + jsdom + `@testing-library/react` + `vite-tsconfig-paths` + `vitest-axe`; `test` / `test:watch` scripts; helpers under `tests/_helpers/`; **T30, T31 pass** (7 tests). D-57: manual `expect.extend` for vitest-axe v0.1.0. D-58: `makeDb()` inline S2 DDL. D-61: Biome ignores `.claude`/`.cursor`. Handoff: **`/implement S1`**. Decisions D-57 → D-61. |
| S010 | `/implement S1` — Affordability domain (pure) | `.specstory/history/` (SpecStory on window close) | `docs/ai/sessions/S010-implement-s1.md` | `5847307` (merged PR #13) | **Complete.** S1 shipped: `lib/affordability/` (types, calculator, validation, copy, framing), `lib/personas.ts` for T8, synthetic fixtures in `tests/_fixtures/ie.ts`; **T1–T8, T29 pass** (30 new tests, 37 total). Added `zod`. D-62: personas in S1 for T8 before S3. D-63: `variable?: boolean` on earners. Handoff: **`/implement S2`**. Decisions D-62 → D-64. |
| S011 | `/implement S2` — Persistence layer (SQLite + Drizzle) | `.specstory/history/` (SpecStory on window close) | `docs/ai/sessions/S011-implement-s2.md` | (uncommitted) on `main` | **Complete.** S2 shipped: `lib/db/` (schema, client, migrate, snapshots), committed `drizzle/` migration, `makeDb()` wired to production path; **T9–T12 pass** (41 tests total). Added `drizzle-kit`. D-65: `rowid` ordering tiebreaker. D-66: `reasons` rebuilt via `assess(ie)` on read. Handoff: **`/implement S3`**. Decisions D-65 → D-66. |
| S012 | `/implement S3` — Persona fixtures + cookie mock auth | `.specstory/history/` (SpecStory on window close) | `docs/ai/sessions/S012-implement-s3.md` | (uncommitted) on `main` | **Complete.** S3 shipped: `lib/identity/persona-cookie.ts` (`getPersonaId` / `setPersonaId`), `lib/db/seed.ts` + seed-on-first-open, **T13–T14 + seed integration pass** (56 tests total). D-67–D-70. Middleware redirect deferred to S4/S8 (documented in session). Handoff: **`/implement S9`**. |
| S013 | `/implement S9` — Reflection-not-advice framing | `.specstory/history/` (SpecStory on window close) | `docs/ai/sessions/S013-implement-s9.md` | `591ff1c` (merged PR #17) | **Closed.** S9 shipped: `components/FramingNotice.tsx` (uses existing `lib/affordability/framing.ts` from S1), **T43 + T32 pass** (58 tests total). D-71–D-73. `@critic` Minor fixes applied (T32 labelledby wiring, T43 href scan + spec comment, known R9/SC 2.5.8 limits). Handoff: **`/implement S4`**. |
| S014 | `/implement S4` — Affordability surface (dashboard) | `.specstory/history/` (SpecStory on window close) | `docs/ai/sessions/S014-implement-s4.md` | (uncommitted) on `main` | **Closed.** S4 shipped: `<DashboardView />`, `<SupportSignpost />`, `src/app/dashboard/page.tsx`, `lib/dashboard/computeDelta.ts`; signpost copy variants in `copy.ts`; **T21–T23, T33, T34, T22, T28 (dashboard half), T44, T45 pass** (91 tests after critic follow-up). D-74–D-76. `@critic` Minor fixes applied (`computeDelta` tests, R6 disposable sign, T33 hardening). Handoff: **`/implement S5`**. |
| S015 | Post-S4 fixes (DB init, persona picker, support route, delta copy, integer math, T12, invalid persona) | `.specstory/history/` (SpecStory on window close) | `docs/ai/sessions/S015-fixes.md` | `3be2735` (merged PR #20) | **Closed.** Seven fixes: `.data/` mkdir before SQLite open; persona picker on `/` + `selectPersona` action; `/support` page; `no-snapshot` delta distinct from `first-snapshot`; integer near-breakeven threshold; T12 aggregates all `console.*` spies; invalid persona cookie redirects to `/`. **98 tests pass.** Handoff: **`/implement S5`**. |
| S016 | `/implement S5` — Snapshot submission flow + delta | `.specstory/history/2026-06-11_08-36-25Z-s5-implementation.md` | `docs/ai/sessions/S016-implement-s5.md` | (uncommitted) on `main` | **Closed.** S5 shipped: `<UpdateForm />` (responsive stacked/grid, table layout dropped), `src/app/dashboard/update/` page + `updateSnapshotAction` (rejects missing **and** invalid persona), `lib/update/` (string-based pounds→pence parser, copy, types, fieldIds); **T18–T20, T24–T25, T35–T38, T36 + parse-pounds-to-pence pass** (122 tests total). D-77–D-81. User-feedback round (D-80): blank amount = field error, T19 covers negative/non-numeric/blank, T35 covers missing + invalid persona, T18 asserts full IE shape. `@critic` Minor fixes applied (D-81): distinct add-row aria-labels per spec line 299, `requiredNote` rendered in visible text per line 303, T25 extended with `fireEvent.change` typed-input survival case. Deferred (not must-fix): T19/fixture backfill to TEST_PLAN, action `_` message under T38 scan, missing-vs-invalid persona message differentiation. Handoff: **`/implement S6`**. |
| S017 | `/implement S6` — History view (+ critic round) | `.specstory/history/2026-06-11_10-09-59Z-historylist-component-implementation-and-tests.md` | `docs/ai/sessions/S017-implement-s6.md` | (uncommitted) on `main` | **Closed.** S6 shipped: `components/HistoryList.tsx` (sync, props-only; empty + populated states; `<ol>` of rows with `<time dateTime>`, disposable + band chip suppressed for no-data rows, `<details>/<summary>` disclosure containing `<dl>`/`<dt>`/`<dd>` IE breakdown, `<SupportSignpost />` + `<FramingNotice />` rendered inside both states), `src/app/history/page.tsx` (async I/O wrapper, persona-cookie redirect). **T26, T27, T28 (history half), T39 pass** (131 tests total, +9). D-82–D-90. `@critic` Verdict: **Ship as-is**, no must-fix; user applied D-91 (F-M1 + G1): swapped `<li aria-label>` for `<li aria-labelledby>` pointing at the in-row `<time>` + outcome state span, and extended T26 to assert per-row outcome state label is present per outcome state. 131 tests still pass; typecheck and S6-scoped lint clean. Deferred (out of slice): T40/T41/T42 manual checklists belong to **S8**; async-page unit tests deferred per tech-spec §4. `npm run lint` has three pre-existing repo-wide formatter nits in S3/S4 files — outside this slice's scope. Handoff: **`/implement S8`**. |
| S018 | `/implement S8` — Submission deliverables (README, DECISIONS, prompt-history backfill) | `.specstory/history/2026-06-11_11-04-59Z-s8-implementation-details.md` | `docs/ai/sessions/S018-implement-s8.md` | (uncommitted) on `main` | **Closed.** S8 shipped: replaced `create-next-app` scaffold `README.md` with reviewer-first runbook (install / migrate / seed / dev / test / build, persona table, route table, where data lives, where AI history lives, links to PRD / TECH_SPEC / TEST_PLAN / DECISIONS / discovery NOTES); new `DECISIONS.md` at repo root (what was built with `S*` refs, what was left out — Stretches R11/R12/R13 + tech-spec deferrals + PRD non-goals + withdrawn TASK_ANALYSIS, what is next, why — links PRD non-goals + tech-spec §5 trade-offs + critic rounds + append-only ID discipline, time-spent table per R17 ≈ 23 hours across 18 sessions); flipped T40/T41/T42 status from `Pending` → `Pending (manual)` in `docs/TEST_PLAN.md` §7 to reflect that S8 has shipped the artefacts these manual checklists verify; appended this S018 row. **Test counts unchanged at 131** (S8 is documentation-only). `npm test` 131/131; `npm run typecheck` clean; `npm run lint` clean (the three pre-existing repo-wide formatter nits S017 noted appear to have been resolved in-flight, likely in the S017 merge — verified with `biome check` reporting no fixes needed). No new code, no new dependencies, no new tests. Decisions D-92 → D-96. Handoff: **first pass closed**; user has signalled an upcoming UI-polish session, after which **S8 will be re-run** to refresh `README.md` (Routes / Personas tables) and `DECISIONS.md` (what was built + time-spent table) — per tech-spec §S8's "can also be re-run between implementation slices to keep the README accurate". Remaining optional follow-ups (Playwright async-page slice, R11 currency migration, R12 share-link with threat model, per-line delta for R2, tone-token lexicon expansion for R20) are listed under "What is next" in `DECISIONS.md`. |
| S019 | UI / UX polish + iteration (five passes: original polish → S019-fixes → `/implement S8` re-verification → critic-driven fixes → user-driven UX iteration; three documented scope exceptions: D-119 / D-123 / D-128 with TECH_SPEC amendments queued for a future `/tech-spec` round) | `.specstory/history/` — SpecStory captures one file per Cursor window on close (four windows for this session: original polish ~13:00 UTC+1, S019-fixes follow-up ~14:00 UTC+1, critic-driven fixes ~15:00 UTC+1, user-driven UX iteration ~16:00 UTC+1) | `docs/ai/sessions/S019-ui-polish.md` | uncommitted on `main` at session close; commit SHAs recorded against the PR when branched | **Closed.** **Original polish pass** — visual / interaction work across the shipped surfaces: applied the loaded Geist font for real (was falling back to Arial), calmer slate CSS-variable palette with dark-mode parity, fade-in utility respecting `prefers-reduced-motion`, new `components/AppHeader.tsx` (server component reading existing `getPersonaId()` — no new auth surface) with brand mark + persona-aware Dashboard / Update / History / Support nav + active-persona chip + "Switch persona" link; persona picker on `/` converted from `<select>` to a real radio group (`<fieldset legend="Persona">` + `<ul>` + `<label>`-wrapping-`<input type="radio">` rows) preserving the existing `selectPersona` Server Action; only structural test that changed is `tests/s3/persona-picker.test.tsx` (now asserts `role="group"`, exact radio count, `name="personaId"`); `<DashboardView />` got a snapshot hero with first-name greeting (derived from `persona.label`) + three metric cards (income / outgoings / disposable) + a two-up reasons / delta layout, all `lucide-react` icons `aria-hidden`, `data-testid` hooks preserved; `<UpdateForm />` got softer card treatment + icon-backed Add / Remove buttons with the same `aria-label`s + clearer primary/secondary submit/cancel hierarchy, same fieldsets / legends / labels / error summary / focus behaviour / value preservation / row management / validation / Server Action; `<HistoryList />` got a "Back to dashboard" link, a soft-bordered card timeline, side-by-side Income / Outgoings sections inside each disclosure; `<SupportSignpost />` + `<FramingNotice />` got card surfaces with `lucide-react` discs (copy / landmarks / links unchanged); `/support` got the same card system with `<h1>Support</h1>` kept so the S3 support-page test still passes. Added `lucide-react@^1.17.0`. Decisions D-97 → D-103. **S019-fixes follow-up** (same session, after user review): (1) removed `sticky top-0 z-20 ... backdrop-blur` from `<AppHeader />` — keeps `docs/TECH_SPEC.md` §4's SC 2.4.11 (Focus Not Obscured) satisfied by construction (D-104); (2) anonymised the SQLite path log in `lib/db/migrate.ts` from `db: opened path=${dbPath}` to `db: opened` — tightens R10 data-minimisation against absolute-path / home-dir leakage (D-105); (3) refreshed `README.md` with a new **UI surface** section describing `<AppHeader />` / radio-card persona picker / dashboard hero metrics / history timeline cards / `lucide-react` iconography, and pointed the **Where the AI workflow lives** section at tracked `.rulesync/` + `AGENTS.md` instead of gitignored `CLAUDE.md` / `.cursor/` outputs (D-106); (4) refreshed `DECISIONS.md` "What was built" to record S019 as a non-feature polish pass, updated the time-spent table to 19 sessions / ~24 hours; (5) flipped `docs/TEST_PLAN.md` §7 status from `Pending` → `Implemented` for every `T*` that auto-passes (T1–T14, T18–T39, T43–T45) while keeping **T15–T17 as `Reserved`** and **T40–T42 as `Pending (manual)`** (D-107); (6) replaced `TBC` raw-transcript / commit placeholders in this row and in the S019 snapshot. All 131 tests still pass; `npm run lint` / `npm run typecheck` / `npm run build` clean. Decisions D-97 → D-108. **`/implement S8` re-verification (third pass within S019)** — audited every S8 deliverable in-place; one stale gitignored reference in `README.md` ("Personas" section pointed at `.cursor/rules/10-evidence.mdc`) was repointed at the tracked `.rulesync/rules/10-evidence.md` (D-109). No code touched, no test count change. **`@critic` review + fixes (fourth pass within S019)** — D-110 restored `docs/TECH_SPEC.md` §S4 bullet 1 inside `<DashboardView />` (persona-name line + Switch persona link); D-111 bumped the variable-earner checkbox to 24×24 for SC 2.5.8; D-112 added `aria-current="page"` to `<AppHeader />` nav (extracted into `<AppHeaderClientRegion />` via `usePathname()`); D-113 removed a dead conditional in `<SupportSignpost />`; D-114 lifted `extractFirstName` to `lib/personas#personaFirstName`; D-115 tightened `--border-strong` to slate-500 (≈4.6:1 light / ≈4.8:1 dark) for SC 1.4.11 3:1; D-116 threaded a `testId` prop through `<SnapshotMetric>` and removed the `display:contents` wrapper; D-117 annotated TEST_PLAN T40 / T41 / T42 with the S019 re-verification note; D-118 added a 19-vs-20-session reconciliation footnote in `DECISIONS.md`; **D-119 recorded D-105 (DB-log anonymisation) as the first deliberate scope exception with a queued §S2 TECH_SPEC amendment**. **User-driven UX iteration (fifth pass within S019, D-120 → D-128)** — D-120 merged hero greeting + Switch persona inline; D-121 added a real cookie-clearing `switchPersona` Server Action in `src/app/actions.ts` (paired with new `clearPersonaId` in `lib/identity/persona-cookie.ts` and an extended `delete()` mock in `tests/_helpers/withPersonaCookie.ts`); D-122 made `<AppHeader />` path-aware (suppresses persona-aware UI on `/` via the new `<AppHeaderClientRegion />`); **D-123 removed the inline Switch persona from the hero (single owner is now `<AppHeader />`) — second deliberate scope exception, §S4 bullet 1 amendment queued**; D-124 extracted a shared `<BackToDashboardLink />` (HistoryList + UpdateForm); D-125 right-aligned UpdateForm's "Add another …" buttons; D-126 added `sm:rounded-none sm:border-0` to UpdateForm rows so the card treatment is mobile-only; D-127 relocated the dashboard-actions nav into the hero `<section>`, right-aligned, primary CTA trailing with `ArrowRight`; **D-128 data-driven the new-customer experience — third deliberate scope exception, §S4 bullet 7 amendment queued**: `<AppHeader />` reads `getLatestSnapshot(persona.id)` and hides Update + History when there are no snapshots, `<DashboardView />` hides "View past submissions" and renames the primary CTA `Update` → `Add` until the first snapshot exists. All five passes verified at close-out: `npm test` 131 / 131 across 48 files, `npm run lint` clean (110 files, +`BackToDashboardLink.tsx`, replaced `AppHeaderNavLink` with `AppHeaderClientRegion`), `npm run typecheck` clean, `npm run build` clean (6 server-rendered routes). Decisions D-97 → D-128. Handoff: **submission-ready as of S019 close**; next session should be a `/tech-spec` round to land the three queued amendments (§S2 DB log, §S4 bullet 1 affordance split, §S4 bullet 7 verb + no-data clause) so the spec re-converges with the implementation. Remaining optional follow-ups (Playwright async-page slice, R11 currency migration, R12 share-link with threat model, per-line delta for R2, tone-token lexicon expansion for R20, `next/link` adoption across the polish surfaces) stay listed in `DECISIONS.md` "What is next". |
| S021 | `/tech-spec` — append-only stretch-blocker round (S2 / S10 / S11 / S12 / §7 only; PRD untouched; MVP design / §2 architecture untouched; implementation code untouched) + same-session `@critic` close-out | `.specstory/history/2026-06-11_17-24-32Z-tech_spec-blockers-and-document-updates.md` | `docs/ai/sessions/S021-tech-spec-stretch-blockers.md` | uncommitted on `feat/s020-tech-spec-stretch-addendum` at session close; start commit `37b8e69` (chore: capture S020 SpecStory raw transcript — branch tip when S021 opened) | **Closed (documentation-only, post-critic).** `docs/TECH_SPEC.md` bumped through **revision 5 (S021 base)** and **revision 5.1 (S021-fix close-out)** in the same Cursor window, both append-only inside S2 / S10 / S11 / S12 / §7. **Revision 5 (D-152 → D-161):** **D-152** S12 PDF API verb pinned — `pdf().toBuffer()` wording replaced with `@react-pdf/renderer`'s documented top-level Node helper `renderToBuffer(<SnapshotPdf />)`; local wrapper `lib/pdf/render.ts#renderSnapshotPdfToBuffer` delegates. Verification list tightened for `/implement S12` (pin exact `x.y.z`, confirm export from package root, no Chromium binary). **D-153** S12 Route Handler `runtime = 'nodejs'` pinned with a static-import test row. **D-154** S11 + S12 persona-cookie validation through `getPersonaById(personaId)` — missing or invalid id rejected before any DB read; no `share_links` write (S11), no `renderSnapshotPdfToBuffer` invocation (S12). Three new test sub-cases each (cookie absent / empty / not-a-persona). **D-155** S11 resolver explicitly handles snapshot-row-missing as a third miss arm rendering the same `<ShareUnavailable />` with the same response headers as unknown-token + expired-token. **D-156** S11 cache / indexing posture made explicit: `/share/[token]` dynamic + non-cacheable; `<SharedStatementView />` and `<ShareUnavailable />` send `Cache-Control: no-store, private`; shared pages send `X-Robots-Tag: noindex, nofollow` HTTP header + matching `<meta name="robots">` via `generateMetadata`; `/share/` added to `robots.txt`. **D-157** S11 `AppHeader` posture made explicit (initial two-option form — route-group / layout separation OR conditional render). **D-158** S2 logging-hygiene wording tightened — removed `db: opened path=<path>` example; allowed lifecycle line is `db: opened local sqlite database` (no path) or no DB-open log at all. **D-159** S11 clock control made explicit — new `lib/share/clock.ts` exports `nowUtc()` used by mint + resolver; both `vi.mock` and `vi.useFakeTimers` styles must work. **D-160** S10 integer-pence display-only invariant recorded — `formatMoney`'s `pence / 100` divide is display-only; all affordability arithmetic stays in integer pence. **D-161** S12 PDF test scope note added — `T*` rows assert required-content presence via text extraction, never exact pixel layout / kerning / line wrap / page count / byte equality. **Same-session `@critic` round + revision 5.1 close-out (D-162 → D-175):** verdict **BLOCK** on a single Blocker — **F1.1** (S11 page commits to response headers but Server Components cannot set them per `node_modules/next/dist/docs/01-app/03-api-reference/04-functions/headers.md`) — plus 7 should-fixes and 6 nits. User chose to close all 14 inside the S021 scope (S2 / S10 / S11 / S12 / §7); F1.6 + F1.7 closed via the in-spec "conscious reading" pattern (mirrors R20 / R7 broadening at S11 `<SharedStatementView />` + S12 PDF) rather than a `/prd` round. **D-163 (F1.1 — Blocker closed):** S11 names project-root `middleware.ts` matched on `/share/*` as the response-header origin (`NextResponse.next({ headers: { 'Cache-Control': 'no-store, private', 'X-Robots-Tag': 'noindex, nofollow' } })`); cross-references S3's existing middleware seam. Test row rewritten to import the middleware function directly — structural header-parity across the four arms is one assertion (pathname-driven matching covers them all). Negative test added (no headers on `/`, `/dashboard*`, `/history`, `/dashboard/snapshot/<id>/pdf`). **D-164 (F1.2):** `resolveShare(token, now): Promise<Snapshot \| null>` declared at `lib/share/resolve.ts` with all-three-miss-arms-collapse-to-null contract; page bullet rewritten to delegate (no more inlining of `hashShareToken` / `getShareLinkByTokenHash` / `getSnapshotById` in the page). **D-165 (F1.3 + F1.8):** `<AppHeader />` codified as pre-existing surface (not retro-added to an MVP slice — would breach "MVP design unchanged"); conditional-render-inside-`<AppHeader />` enforcement shape **dropped**; route-group / layout separation under `app/(share)/...` is now the only acceptable form; pathname assertion narrowed to subtree-scoped + static import-graph check. Status block closure (v) updated for consistency; stale prose pruned. **D-166 (F1.4):** `getPersonaById(id): Persona \| undefined` declared as an additive extension to `lib/personas.ts` (same append pattern as `getSnapshotById` on `lib/db/snapshots.ts`); one declaration covers S11 + S12; `undefined`-on-miss for clean call sites. **D-167 (F1.6 + F1.7 — in-spec, no /prd round):** cache/indexing posture and `AppHeader` posture each get a closing R10 + R12 joint-broadening sentence (mirrors R20/R7 conscious-reading pattern); §7 R10 row extended with both broadenings + the explicit "if a future PRD revision wants to narrow, that is a `/prd` change" cite-back. **D-168 (F1.9):** `withPersonaCookie(null)` sub-case reframed as "cookie absent" (helper not called) to match the actual S7 helper semantics. **D-169 (F1.10):** §7 R6 row extended to list S12 (PDF reuses S1 + S9 strings; tone-token guard inherits coverage automatically). **D-170 (F1.11):** S7 S2-logging-hygiene row rewritten from "any character of the resolved DB path" to "any **substring** of the resolved DB path" — substring search on the path string in full, not per-character membership. **D-171 (F1.12):** S12 PDF test-scope note adds tagged-PDF structure (WCAG SC 1.3.1 / 1.3.2) to the exclusion list. **D-172 (F1.13):** S11 persona-validation test row drops the vacuous "or `snapshots`" assertion (`createShareLinkAction` never writes to `snapshots`). **D-173 (F1.14):** `share_links.id` annotated in-schema as the reserved future revocation handle (couples to the deferred-revocation trade-off so the schema is forward-leaning). **D-174 internal consistency sweep:** status block bumped to revision 5.1 with full critic-findings-closed paragraph; closure (v) updated to single enforcement shape; stale `app/share/[token]/page.tsx` reference updated to `app/(share)/share/[token]/page.tsx`; brittle hard-coded `:646` cross-reference made descriptive; lints clean. **D-175 not findings:** carried forward unchanged from S020 (R20/R7 broadening; access-log limitation under R10; coercion / forwarded-under-pressure suspicion; `@react-pdf/renderer` "lightweight" claim verifiable at `/implement S12`; no-`T*` for end-to-end `useActionState`; §5 `pdf(element).toBuffer()` mention as "still exported"). **Net spec change:** 805 → 850 lines. **No PRD edits, no implementation code, no MVP design edits, no §2 architecture edits, no test counts changed.** Handoff: **`/test-plan` is now unblocked for stretch coverage** (S10 / S11 / S12); F1.1 was the single Blocker and is closed via the middleware-unit assertion shape — `T*` insertion points T15–T17 in `docs/TEST_PLAN.md` §7 with notes for `/test-plan` (one middleware-unit row covers all four arms structurally; `<meta robots>` is a separate `generateMetadata` row; clock helper has two valid mock styles; logging-hygiene assertions are application-code-only; persona-validation has three sub-cases each; PDF tests assert presence not layout, tagged-PDF excluded). **`/implement S10` is the smallest stretch entry-point** and unblocks `formatMoney` for S11 + S12; **`/implement S12` must verify `@react-pdf/renderer`** before pinning. The three queued S019 TECH_SPEC amendments (§S2 DB-log anonymisation, §S4 bullet 1 affordance split, §S4 bullet 7 verb / no-data clause) remain untouched and still need their own `/tech-spec` round. |
| S022 | `/test-plan` — stretch extension (S10 / S11 / S12 test commitments translated to T46–T76; MVP T1–T45 untouched; PRD / tech-spec untouched; implementation code untouched) | `.specstory/history/` — SpecStory writes one file per Cursor window on close | `docs/ai/sessions/S022-test-plan-stretch.md` | uncommitted on `feat/s020-tech-spec-stretch-addendum` at session close; start commit `e3dccc8` (S021 close-out — `docs/TECH_SPEC.md` rev 5.1 committed) | **Closed (documentation-only).** `docs/TEST_PLAN.md` extended append-only: header `Inputs consumed:` line now cites S10 / S11 / S12 at rev 5.1; `Status:` line records the S022 stretch extension; new gate-criteria bullet for stretch (R19 designed-conditional). **31 new `T*` rows appended (T46–T76)** lifted directly from tech-spec §3 "Tests (R19)" commitments at rev 5.1 — **S10 T46–T51** (6 rows): migration / default backfill / repository round-trip / `formatMoney` helper / integer-pence invariant / logging hygiene; **S11 T52–T67 + T76** (17 rows): token generation / migration / repository round-trip + expiry / `nowUtc()` clock helper with two mock styles / Server Action happy + ownership + persona validation three sub-cases / `resolveShare` happy + three miss arms collapse to `null` / `middleware.ts` cache + robots headers on `/share/*` only (resolves F1.1 at test-plan layer) / `generateMetadata` robots / no-static-render scan / `<SharedStatementView />` + `<ShareUnavailable />` + `<ShareSnapshotForm />` render + a11y + subtree-scoped persona-leak DOM contract (resolves F1.3 + F1.8) / logging hygiene application-code scope / `<ShareUnavailable />` copy tone guard (separate row, no T29 extension); **S12 T68–T75** (8 rows): Route Handler `runtime = 'nodejs'` / happy-path response shape with `Content-Type` + `Content-Disposition` + `Cache-Control: no-store, private` + `%PDF-` byte prefix / cross-persona 404 byte-equality / persona validation missing + invalid / `renderSnapshotPdfToBuffer` smoke + outcome-state content coverage (presence not layout) / `formatMoney` integration money-string drift guard / logging hygiene application-code scope / no-`fs.writeFile`-during-GET spy. **Two stretch-wide guardrails recorded in §1** rather than per-row: (a) R10 logging-hygiene scoped to application-code only — no `T*` against framework / proxy / CDN access logs (per tech-spec §5 "S11 + S12 access-log limitation under R10"); (b) S12 PDF tests assert **presence not layout** — no `T*` against pixel layout, kerning, line wrap, page count, byte equality, or tagged-PDF semantic structure (per tech-spec §S12 test-scope note + §5 "S12 no tagged-PDF"). **Decisions:** D-177 T15–T17 stay `Reserved` (strict-append-only from T46 chosen over the S021 handoff's insertion-point suggestion); D-179 every stretch row cites R19 alongside its feature `R*`; D-181 F1.1 mirrored via T61 middleware-unit assertion (single row, structural header-parity across the four `/share/*` arms; negative arms `/`, `/dashboard*`, `/history`, `/dashboard/snapshot/<id>/pdf` pass through); D-182 F1.3 + F1.8 mirrored via T64 / T65 subtree-scoped DOM contract + static import-graph scan for `<AppHeader />` absence under `app/(share)/**`; D-183 persona-cookie validation three sub-cases (cookie absent helper-not-called / cookie present empty / cookie present not-a-persona) for both T58 (S11) and T71 (S12); D-184 three resolver miss arms collapse to `null` with cross-arm strict-equality (T60); D-185 both `vi.mock` and `vi.useFakeTimers` styles must pass against the same fixtures (T55); D-187 `<ShareUnavailable />` deliberately excluded from R20 + R7 — T65 asserts framing notice + signpost absent (mirrors S007 round-2 F2.1 narrowing); D-188 R6 stretch tone guard gets its own row (T76) rather than extending T29; D-189 stretch fixtures in §2.3 with `share*` / `pdf*` / `pinned*` prefixes deliberately distinct from MVP fixture names; D-190 §6 stretch out-of-scope items mirror tech-spec §6 carry-outs (13 bullets); D-191 §7 status legend gains `Pending (stretch)` as a fourth value; D-192 header status bumped + stretch gate clause added. **§4 edge cases** appended with 17 stretch scenarios mapping to T* rows. **§5 coverage matrix**: R11 / R12 / R13 rows moved from `Gap (intentional)` to listing T46–T75; R19 moved from `Gap (conditional)` to "T46–T76 (every stretch row carries an R19 citation)"; R20 row extended with stretch broadening note (T64 + T72) and the `<ShareUnavailable />` exclusion called out; Must-requirement gate unchanged; Should-requirement gate notes R19 designed-conditional + R6 / R7 / R10 / R18 / R20 stretch broadening; new "Stretch gate" sentence. **§6** appended with stretch-specific out-of-scope items (framework access logs, S11 single-use / revocation / rate limiting / wire-layer timing, S12 tagged-PDF / pixel layout / page count / byte equality, S11 ↔ S12 cross-integration, `@react-pdf/renderer` verification as `/implement S12` pre-check, coercion / forwarded-under-pressure suspicion, end-to-end `useActionState` runtime for `<ShareSnapshotForm />`, async Server Component page integration, async Route Handler binding). **§7 traceability** extended with three new tech-spec → test-plan rows (S10 / S11 / S12); test-plan → implementation status table extended with 31 new `Pending (stretch)` rows (T46–T76, each citing `R*` + `S*`). **Net plan change:** 637 → 1143 lines. **No PRD edits, no tech-spec edits, no implementation code, no executable tests run (existing MVP suite at 131 tests remains untouched).** Handoff: **`/implement S10` is the next stretch entry-point** (smallest slice, ships T46–T51 alongside `lib/affordability/format.ts` + Drizzle migration); `/implement S11` follows per tech-spec §3 ordering (`S10 → S11 → S12`) and ships T52–T67 + T76; `/implement S12` follows and ships T68–T75 after verifying `@react-pdf/renderer` against installed `node_modules` (pin exact `x.y.z`, confirm `renderToBuffer` exported from package root, confirm no Chromium binary in `node_modules/.bin`). The three queued S019 TECH_SPEC amendments (§S2 DB-log anonymisation, §S4 bullet 1 affordance split, §S4 bullet 7 verb / no-data clause) remain untouched and need their own `/tech-spec` round. Decisions D-176 → D-192. **Same-session `@critic` round + minimum fixes (D-193 → D-203):** critic verdict **Minor fixes** (no Blockers); all six F1.x stretch-accuracy checks passed (F1.1 middleware-unit + parity + negatives; F1.3 + F1.8 subtree-scoped + import-graph; F1.9 cookie-absent reframe; F1.12 tagged-PDF exclusion; F1.13 vacuous-snapshots drop; F1.2 cross-arm `=== null`). Eleven findings closed inside the S022 scope (test-plan-only, append-only, no PRD / tech-spec / code change); the user constraint was "do not deviate from the original PRD and requirement doc" and every fix was cross-checked against PRD R1–R20 and tech-spec rev 5.1 to confirm it is a bookkeeping / clarification / narrowing edit. **D-193 (F1.1 + F2.1):** T72 Covers extended to `R13, R19, R20, R7`; Touches extended with S4; new GWT bullet asserting the support-signpost copy block + `"/support"` URL substring appear in the extracted PDF text (per tech-spec §S12 design bullet 11 + §7 R7 row); §7 status row reconciled. **D-194 (F1.2):** T72 no-data `reasons[]` skip-note rewritten to explain why the exemption holds (S1 commits a single placeholder reason; T1 / T21 already cover the no-data reasons content; T72 does not re-assert it through the PDF surface). **D-195 (F2.2):** T63 Covers extended to `R12, R10, R19`; closing sentence cites the tech-spec §S11 R10 + R12 conscious reading (accidental static pre-rendering of `/share/[token]` would let third-party caches retain shared content); §7 status row reconciled. **D-196 (F4.1):** T50's static-source `/ 100` scan half **dropped**; runtime persona-fixture `Number.isInteger(...)` half kept and extended to also exercise `Delta.disposableDeltaPence` (per tech-spec §S10 "Integer-pence invariant" bullet — `5% × income` comparison and the delta both stay in integer pence); the "no `*Pence / 100` outside `formatMoney`" rule lives at the source-discipline layer, not the test-plan layer, because a developer using `* 0.01` would round-trip-pass anyway. **D-197 (F5.1):** §5 R6 row extended to list T76 with a stretch-broadening note pointing at tech-spec §7 R6 row; S12 explicitly recorded as contributing no new tone surface (PDF reuses S1 + S9 sources under existing T29 / T43 scans). **D-198 (F5.2):** §5 R7 row extended to list T64, T72 with a stretch-broadening note (R7 conscious reading per tech-spec §7 R7 row); `<ShareUnavailable />` explicitly excluded (T65 asserts `<SupportSignpost />` absent — mirrors R20 narrowing). **D-199 (F5.3, highest-impact):** §5 R10 row extended to list 14 stretch `T*` rows (T49, T50, T51, T57, T58, T60, T61, T63, T64, T65, T67, T70, T71, T74, T75) plus the two R10 + R12 conscious-broadening readings already recorded in tech-spec §S11 (cache / indexing posture on `/share/*`; no-persona-identity in recipient-facing DOM under `/share/*`); application-code-only scope for stretch logging hygiene restated with the access-log-layer "no `T*` allocated" guardrail cross-referenced to tech-spec §5 trade-off "S11 + S12 access-log limitation under R10" + §6. Closes the largest "Gaps are explicit" honesty gap the critic flagged — the previous "T6, T12, T20" enumeration silently understated R10's stretch coverage. **D-200 (F5.4):** §5 R18 row extended to list T64, T65, T66 with a stretch-broadening note that distinguishes load-bearing (T64) from completeness (T65 — trivially passes; T66 — `<ShareSnapshotForm />` SC 2.5.8 24×24 + `aria-describedby` to expiry); S12 PDF tagged-PDF (SC 1.3.1 / 1.3.2) explicitly recorded as a carry-out per tech-spec §5 trade-off "S12 no tagged-PDF" + §6. **D-201 (F5.5):** §5 R20 row extended to list T76; row body distinguishes the **framing-presence half** of R20 (T64 + T72; `<ShareUnavailable />` excluded) from the **advice-implying token half** of R20 (T76 paired with R6 on `<ShareUnavailable />`'s new copy string). **D-202 (F7.1):** §2.3 `snapshotJordanStoredGbp` Used-by column expanded to list every `T*` that consumes the fixture (T47, T48, T56, T57, T59, T60, T64, T67, T69, T72, T73, T74, T75) — previously omitted T57 / T60 / T67 / T74 / T75. **D-203 (F7.2):** §2.3 `mockClock` entry rewritten with an explicit "Implementation note for `/implement S11`" describing Vitest's `vi.mock(...)` hoisting behaviour and the two acceptable shapes (factory-returning helper with top-level `const pinned`, or inline `vi.mock(...)` per test file); the fake-timers alternative preserved as the second valid style. **Suspicion-flagged items (not closed, carried to `/implement`):** `pdfTextExtractor` reliability against `@react-pdf/renderer` output (deterministic for the `<SnapshotPdf />` layout `/implement S12` writes? if not, `T*` rows might need a fuzzy-match helper rather than strict `.toContain(...)`); whether `getPersonaById` warrants a dedicated `T*` row beyond indirect coverage via T58 / T71. **Consciously not raised** (12 items recorded in the S022 snapshot): T15–T17 reservation honoured; `force-dynamic` permitted-not-required; clock-mock style not pinned per `T*`; no stretch tone guard for `<SharedStatementView />` (reuses S4 strings); etc. **Net plan change after critic round:** `docs/TEST_PLAN.md` only (T63 + T72 row Covers extended; T50 row narrowed; T72 + T50 row text rewritten; §5 R6 / R7 / R10 / R18 / R20 rows extended; §2.3 `snapshotJordanStoredGbp` + `mockClock` rows extended; §7 status rows for T63 + T72 reconciled). **No new `T*` IDs allocated, no row removed, no MVP row touched. No PRD edits, no tech-spec edits, no implementation code, no executable tests run.** Critic transcript: agent `2ae20b0d-c13e-4fac-adfa-34d6392a9cb0`. Decisions D-176 → D-203. |
| S020 | `/tech-spec` — append-only stretch addendum (S10 / S11 / S12 designed; PRD / S1–S9 / §2 architecture untouched) | `.specstory/history/` — SpecStory writes one file per Cursor window on close | `docs/ai/sessions/S020-tech-spec-stretch-addendum.md` | uncommitted on `main` at session close; start commit `32c1264` (Merge PR #25) | **Closed (documentation-only).** `docs/TECH_SPEC.md` bumped to revision 4 with three new stretch slices appended to §3: **S10 — currency / country_code** (R11, R19): Drizzle migration adds `currency TEXT NOT NULL DEFAULT 'GBP'` + `country_code TEXT NOT NULL DEFAULT 'GB'`, `Snapshot` type narrowed to `'GBP'`/`'GB'` literals, new `lib/affordability/format.ts#formatMoney` helper, repository / call-sites rewired without changing the rendered string. **S11 — secure time-limited statement sharing** (R12, R19): random 32-byte `base64url` bearer token + SHA-256 hex hash stored in a new `share_links` table; raw token never persisted; ownership-checked at mint via `snapshot.customerId === personaId`; resolver returns the same `<ShareUnavailable />` page for missing / expired / unknown tokens (no information leak); new components `<SharedStatementView />`, `<ShareUnavailable />`, `<ShareSnapshotForm />`; threat-model summary inline; single-use, revocation, rate limiting deferred. **S12 — PDF export** (R13, R19): Next.js 16 Route Handler at `app/dashboard/snapshot/[id]/pdf/route.ts` using `@react-pdf/renderer` (lightweight, no Chromium binary, no Playwright dependency); ownership-checked GET that streams the buffer with `Content-Type: application/pdf` + `Content-Disposition: attachment` + `Cache-Control: no-store, private`; PDF contents = branding / snapshot date / currency / country / income / expenditure / disposable / band / reasons / breakdown / support signpost / framing notice (reuses S10's `formatMoney` and S9's `framing.ts`); no PDF storage; tagged-PDF deferred. §5 trade-offs extended (S10 type narrowing, S10 locale derivation, S11 random+hash vs HMAC, S11 single-use deferred, S11 revocation deferred, S11 rate limiting deferred, S11 same-response-on-miss, S11 reintroduces an HTTP surface — MSW reconsidered, S12 lib choice, S12 no tagged-PDF, S12 no storage; the previously-deferred "Stretch security model for R12" entry is now resolved by S11). §6 out-of-scope refreshed: R11 / R12 / R13 lines updated from "not designed" → "designed in S10 / S11 / S12 — not delivered until /implement runs"; new entries for S11 rate limiting, S11 single-use / revocation, S12 tagged-PDF, S11 + S12 cross-integration. §7 traceability: R11 → S10, R12 → S11, R13 → S12, R19 → S10 / S11 / S12 (test-discipline). Each stretch slice carries its own coverage commitments (migration / repository / `formatMoney`; token / repository / Server Action / resolver / components / a11y; ownership / handler / `%PDF-` byte-prefix / outcome coverage / `formatMoney` integration / no-file-write spy). **No PRD edits, no implementation code, no Playwright, no auth, no email / SMS, no MVP architecture changes.** Existing S1–S9 sections, §2 architecture, MVP-scope §5 / §6 lines, and the 131-test MVP suite are untouched; `npm test` not re-run because no executable code changed. Decisions D-129 → D-138. **Same-session `@critic` round + fixes (D-139 → D-151, no PRD changes):** critic verdict **Minor fixes**; user accepted all top-3 + named smaller items, applied tech-spec-only. **D-140 mandatory drop:** removed `<FramingNotice />` (and confirmed no `<SupportSignpost />`) from `<ShareUnavailable />` — the page carries no outcome so R20 / R7 do not attach (mirrors S007 round-2 F2.1 narrowing). **D-141 conscious-broadening recorded:** R20 + R7 placement on `<SharedStatementView />` (S11) and the PDF (S12) is now an explicit-conscious-reading of "every outcome screen / surface" with reasoning inlined in §3 S11 + §3 S12; §7 R7 + R20 traceability rows extended to list S11 + S12 with the `<ShareUnavailable />` exclusion called out. **D-142 R10 scoped to application code:** §3 S11 + §3 S12 "Data hygiene (R10)" sub-blocks split into "Application-code commitments (testable)" and "Known limitation (not closeable in stretch)"; bearer-token-in-URL (S11) + snapshot-id-in-URL (S12) leak into Next.js / proxy access logs explicitly acknowledged with three remediation options recorded and **(declare)** picked; new §5 trade-off + new §6 carry-out; logging-hygiene test commitments narrowed to "application-code surface only". **D-143 N1 honesty:** §3 S11 "Threat model summary" reframes the "ownership check at mint" as a UX selector that prevents accidental cross-persona sharing in the demo, not an authenticated authorisation control under N1's unsigned persona cookie; §5 "S11 random bearer token + SHA-256 hash" trade-off extended with the same N1-bound caveat. **D-144 `@react-pdf/renderer` "lightweight" tagged as suspicion** in §3 S12 + §5 — `/implement S12` must pin version, sanity-check install size, confirm `toBuffer` / `toStream` API, confirm no system-browser rasterisation. **D-145 §5 mint vs resolver arms split:** "S11 same response for missing / expired / unauthorised" replaced with "S11 same-response posture (resolver) and same-error posture (mint) — separately"; resolver only sees missing / expired / unknown (no recipient identity); mint is the only place "unauthorised" exists. **D-146 "cannot drift" softened to "money strings cannot drift"** in §3 S12 design + test commitments. **D-147 coercion / forwarded-under-pressure suspicion recorded** in §5 + §6 — three options considered, `(3)` flagged-suspicion picked at the user's "no PRD changes" instruction; `/prd` route deliberately not taken. **D-148 invented `T12` test-ID reference in §3 S10 neutralised** (tech spec now contains zero `T*` references — `/test-plan`'s job). **D-149 §7 traceability extended** for R6 / R7 / R10 / R18 / R20 with stretch-addendum inheritance + the `<ShareUnavailable />` R7 / R20 exclusion + the S12 tagged-PDF carry-out. **D-150 status header bumped** to record the critic round verbatim. **D-151 deferred items** (3): split-out `console.*` spy cases for `/test-plan`, PDF `lang` attribute for `/implement S12`, wire-layer indistinguishability (response-header parity, timing side-channel) for a future security-review round. **Net spec change:** 794 → 805 lines. Handoff: **`/test-plan` is unblocked for stretch coverage** if reviewers want it (T15–T17 reserved range is the natural insertion point for R11 / R12 / R13; do not allocate a `T*` against the framework / access-log layer per the new §5 trade-off); **`/implement S10` is the next stretch entry-point** (smallest slice, unblocks `formatMoney` for S11 + S12); **`/implement S12` must verify `@react-pdf/renderer`** before pinning; the three queued S019 TECH_SPEC amendments (§S2 DB-log anonymisation, §S4 bullet 1 affordance split, §S4 bullet 7 verb / no-data clause) remain untouched and need their own `/tech-spec` round. |
