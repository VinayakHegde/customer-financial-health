# AI Session Snapshot: S002 - AI guardrails (Rulesync)

## Metadata

- Date: 2026-06-10
- Tool: Cursor
- Model: (agent)
- Branch: main
- Start commit: 80bb8302b483bd332bd9260facbf397a46c25642
- End commit: (in progress)
- Raw transcript: `.specstory/history/2026-06-10_11-12-19Z-minimal-rulesync-setup-for-product-development.md`
- Related PRD/spec/test docs: none yet — this session creates the scaffolding that will produce them

## Goal of this Cursor window

Stand up a minimal Rulesync configuration focused on five workflow phases — product discovery, PRD generation, technical specification, test-plan generation, and controlled implementation — without committing to any implementation-specific coding rules yet.

## Context given to AI

- Empty `.rulesync/{rules,commands,subagents,skills}` directories already present in the repo.
- `docs/TASK_ANALYSIS.md` (S001) describes the problem space and recommended MVP. _(Note added retroactively: this artefact was withdrawn in S003 D-14; the brief is now the sole binding source. See `docs/ai/sessions/S003-discovery.md`.)_
- Project ai-history rule mandates curated session snapshots in `docs/ai/sessions/` and updates to `docs/PROMPT_HISTORY.md`.
- `AGENTS.md` notes that this Next.js version may differ from training-data assumptions, so any future implementation rules must defer to in-repo Next docs.

## Main prompts used

User requested a minimal Rulesync setup covering discovery, PRD, tech spec, test plan, and controlled implementation phases, with no implementation-specific rules. Asked for an explained proposal before any files were created. See raw transcript for full detail.

## AI outputs accepted

- `.rulesync/rules/00-workflow.md` (root, gated 5-phase workflow)
- `.rulesync/rules/10-evidence.md` (traceability / anti-fabrication)
- `.rulesync/commands/{discovery,prd,tech-spec,test-plan,implement}.md` (one command per phase)
- `.rulesync/subagents/critic.md` (read-only reviewer)
- `.rulesync/skills/phase-gate/SKILL.md` (shared phase-document schema)
- `rulesync.jsonc` targeting `cursor` and `claudecode` only, with simulation enabled for Cursor commands/subagents/skills.
- Single npm script `rulesync` that forwards all arguments to the CLI (`npm run rulesync -- generate`, `npm run rulesync -- gitignore --targets cursor,claudecode`, `npm run rulesync -- generate --check`, etc.).
- Generated tool files (gitignored): `.cursor/{rules,commands,agents,skills}/`, `.claude/{rules,commands,agents,skills}/`, `.cursorignore`, `CLAUDE.md`.
- Committed tool file: `.claude/settings.json` (Claude Code shared deny list from `.rulesync/.aiignore`).

## AI outputs rejected or changed

- `rulesync init` created sample files (`.rulesync/rules/overview.md`, `commands/review-pr.md`, `subagents/planner.md`, `skills/project-context/SKILL.md`, `mcp.json`, `hooks.json`). All sample files were deleted to keep the setup minimal.
- `CLAUDE.md` (previously hand-authored one-line `@AGENTS.md` redirect) is now generated by rulesync and removed from git tracking with `git rm --cached`. The file remains on disk and is regenerated by `npm run rulesync:generate`.

## Files changed

### Source-of-truth (`.rulesync/`, committed)

- `.rulesync/rules/00-workflow.md` (new)
- `.rulesync/rules/10-evidence.md` (new)
- `.rulesync/commands/discovery.md` (new)
- `.rulesync/commands/prd.md` (new)
- `.rulesync/commands/tech-spec.md` (new)
- `.rulesync/commands/test-plan.md` (new)
- `.rulesync/commands/implement.md` (new)
- `.rulesync/subagents/critic.md` (new)
- `.rulesync/skills/phase-gate/SKILL.md` (new)
- `.rulesync/.aiignore` (new, created by `rulesync init`)

### Configuration (committed)

- `rulesync.jsonc` (new)
- `package.json` (added rulesync devDependency + 3 npm scripts)
- `package-lock.json` (rulesync transitive deps)
- `.gitignore` (rulesync-managed block appended for cursor + claudecode targets)

### Session bookkeeping

- `docs/ai/sessions/S002-ai-guardrails.md` (new)
- `docs/PROMPT_HISTORY.md` (S002 row)

### Generated (gitignored, not committed)

- `CLAUDE.md`, `.cursor/**`, `.cursorignore`, `.claude/rules/**`, `.claude/commands/**`, `.claude/agents/**`, `.claude/skills/**`
- `.claude/settings.json` is **not** gitignored (rulesync's intended team-shared deny list); decide whether to commit it.

## Tests added or run

```bash
npm install -D rulesync                                                    # installed 8.27.0
npx rulesync init                                                          # created .aiignore + samples (samples removed)
npm run rulesync -- generate                                               # wrote 20 tool-native files
npm run rulesync -- gitignore --targets cursor,claudecode                  # appended managed block to .gitignore
npm run rulesync -- generate --check                                       # idempotency: "All files are up to date."
```

## Handoff for next session

1. Decide whether to commit `.claude/settings.json` (rulesync's intended shared Claude Code deny list).
2. Run `/discovery` to capture the first product-discovery pass against `docs/TASK_ANALYSIS.md` as input material. _(Superseded in S003: the official engineering brief (`docs/Ophelos Engineering Take-Home Task.pdf`) was added and used as the sole binding source; `docs/TASK_ANALYSIS.md` was subsequently withdrawn in S003 D-14. See `docs/ai/sessions/S003-discovery.md`.)_
3. Add implementation-specific rules (coding style, file layout, test framework) only when implementation actually starts; remember to run `npm run rulesync -- generate` after any `.rulesync/` edit.
4. Suggested next session ID: **S003 — discovery pass**.
